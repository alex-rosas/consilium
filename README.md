# Consilium — Multi-Agent Compliance Automation

> Three specialized AI agents — Planner, Analyst, Synthesizer — execute sequentially in your browser as a streamed pipeline. Real regulatory documents. Real findings. Every agent decision visible in real time.

![Python](https://img.shields.io/badge/python-3.11+-blue.svg)
![Tests](https://img.shields.io/badge/tests-207%20passing-brightgreen.svg)
![Eval](https://img.shields.io/badge/eval-30%2F30%20(100%25)-brightgreen.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

---

## What Is Consilium?

Consilium is a **multi-agent compliance analysis system** where three specialized AI agents collaborate **sequentially** in a workflow to produce structured regulatory assessments.

**Architecture pattern:** Multi-agent sequential workflow (role-specialized pipeline)  
**NOT:** Ensemble system (parallel models aggregating answers)

```
Query → [PlannerAgent] → [AnalystAgent + Quaestor] → [SynthesizerAgent] → Report
          task plan        retrieve + classify          synthesize
```

Each agent has a distinct role and operates on a shared `WorkflowState` (LangGraph `StateGraph`). No agent calls another agent — LangGraph routes between them.

### Why Not a Simpler Approach?

| Approach | Gap for this domain |
|---|---|
| Single LLM call | No retrieval — hallucinates clause numbers, relies on training-data IFRS |
| RAG only | Retrieves context but produces no structured, per-clause risk assessment |
| Fine-tuned single model | Requires labelled training data per regulatory standard |
| **Consilium** | Decomposes → retrieves from authoritative sources → classifies per clause → synthesises |

The compliance domain specifically requires task decomposition (query → sub-tasks), authoritative retrieval (exact IFRS/PCAOB text, not LLM memory), and structured output (risk-rated findings with citations). A single-call approach fails all three.

### Sequential vs Ensemble — Explicit Contrast

Engineers pattern-match on "multi-agent" to mean ensemble. This is different:

- **Ensemble:** Same question → 3 models in parallel → vote on answer
- **Consilium:** Complex question → 3 agents in sequence → each does one specialised step

Example query: *"Assess JPMorgan Q3 revenue recognition vs IFRS 15"*

| Agent | Role | Output |
|---|---|---|
| PlannerAgent | Decomposes query into max 3 sub-tasks | `["Identify IFRS 15 scope clauses", "Extract Q3 revenue claims", "Assess risk per clause"]` |
| AnalystAgent | Fetches IFRS 15 clauses from Quaestor; classifies each against query | `[FindingCard(clause="IFRS 15.31", risk="High"), …]` |
| SynthesizerAgent | Aggregates findings into Executive Summary + Detailed Findings | Final compliance report |

This is **task decomposition**, not answer aggregation.

---

## Try It

**[→ Live static demo](https://consilium-multiagent.streamlit.app/)** — no setup required.

Pre-recorded responses from the 30-case golden evaluation dataset, covering IFRS 15, PCAOB AS 2810, Basel III, and SEC Reg S-X. Switch between the **Pipeline View** (per-agent JSON inspection + WorkflowState trace) and the **Results** tab (latency scatter, confidence histogram, risk distribution) to explore the architecture.

> All responses are real — generated by actual pipeline runs. No API key, no running services needed.

The full interactive app (requires Groq key + running Quaestor):

```bash
docker-compose --profile app up
# → http://localhost:8001
```

---

## Table of Contents

- [What Is Consilium?](#what-is-consilium)
- [Try It](#try-it)
- [What It Does](#what-it-does)
- [Demonstration — Before and After](#demonstration--before-and-after)
- [How It Works](#how-it-works)
- [Streaming Pipeline](#streaming-pipeline)
- [Results](#results)
- [Stack](#stack)
- [How to Run](#how-to-run)
- [Key Engineering Decisions](#key-engineering-decisions)
- [Known Limitations](#known-limitations)
- [What Was Intentionally Not Built](#what-was-intentionally-not-built)
- [Known Technical Debt](#known-technical-debt)
- [Project Structure](#project-structure)
- [Phases](#phases)
- [Case Study](#case-study)

---

## What It Does

Consilium takes a natural-language compliance query and runs it through a three-agent pipeline backed by real regulatory document retrieval:

- **Decomposes** any query into structured analysis tasks (PlannerAgent — LLM-driven task planning, max 3 subtasks)
- **Retrieves and classifies** relevant regulatory clauses from indexed 10-K filings and IFRS/PCAOB standards, producing risk-rated findings per clause with source citations (AnalystAgent + Quaestor retrieval)
- **Synthesizes** a plain-English compliance report with an Executive Summary and Detailed Findings section (SynthesizerAgent), streamed to the browser live as each agent completes

Indexed documents: JPMorgan Chase Q3 2023 10-Q; Apple FY2025 10-K; IFRS 9/15/16; PCAOB AS 2201.

---

## Demonstration — Before and After

**Query:** *"Identify material misstatement risks in JPMorgan Q3 2023 based on PCAOB audit standards"*

### Phase 0 — Rule-Based Baseline

```json
{
  "findings": [
    {
      "clause_reference": "section_3",
      "finding": "The consolidated statements of income for the three months ended September 30,\
 2023 reflect net revenues...",
      "risk_level": "Medium",
      "document_source": "jpmorgan_q3_2023_10q"
    }
  ],
  "confidence": 0.30,
  "fallback_events": ["analyst"]
}
```

*Clause reference = raw metadata field. Finding = first 100 chars of chunk text. Risk level = keyword match ("audit" → Medium). Every query looks like this regardless of content.*

### Phase 7 — LLM Analysis with Real Retrieval

```json
{
  "findings": [
    {
      "clause_reference": "PCAOB AS 2810.26",
      "finding": "Revenue disaggregation disclosures lack sufficient granularity to assess completeness under AS 2810.26",
      "risk_level": "High",
      "document_source": "jpmorgan_q3_2023_10q"
    },
    {
      "clause_reference": "PCAOB AS 2201.08",
      "finding": "Internal control assessment for revenue reporting processes references prior-period conclusions without current-period evidence",
      "risk_level": "High",
      "document_source": "jpmorgan_q3_2023_10q"
    },
    {
      "clause_reference": "PCAOB AS 2810.44",
      "finding": "Management estimate disclosures for trading asset valuations do not address sensitivity to market inputs as required",
      "risk_level": "Medium",
      "document_source": "jpmorgan_q3_2023_10q"
    }
  ],
  "confidence": 0.85,
  "fallback_events": []
}
```

*Specific regulatory clause references. Evidence-grounded findings. LLM reasoning over retrieved document context. Source-cited.*

| Dimension | Phase 0 (Baseline) | Phase 7 (Final) |
|---|---|---|
| Classification | Keyword matching | Groq llama-3.1-8b — LLM reasoning |
| Clause reference | Raw chunk metadata | Specific IFRS/PCAOB clause (e.g. `IFRS 15.31`) |
| Finding text | First 100 chars of chunk | Evidence + analytical interpretation |
| Confidence | Always 0.30 | 0.85 (LLM) / 0.30 (fallback only) |
| Latency | <50ms (no LLM call) | ~2s end-to-end |
| Streaming | None (blocking JSON) | 5 SSE event types, per-agent live |
| Retrieval | Mock (fixed test chunks) | Real Quaestor (cross-encoder reranked) |
| Tracing | None | Phoenix OTel (per-agent spans) |

---

## How It Works

### Agent Workflow

```
User Query
    │
    ▼
┌──────────────────────────────────────────────────────────────────┐
│ PlannerAgent                                                     │
│ Task: decompose query into ≤3 sub-tasks                         │
│ In:  query string                                               │
│ Out: task_plan[], confidence score                              │
└──────────────────────────────────────────────────────────────────┘
    │  WorkflowState (task_plan, query)
    ▼
┌──────────────────────────────────────────────────────────────────┐
│ AnalystAgent                           ┌─────────────────────┐  │
│ Task: retrieve clauses, classify risk  │   Quaestor API      │  │
│ In:  task_plan[]                   ───▶│  /retrieve          │  │
│ Out: risk_findings[], confidence       │  cross-encoder      │  │
│                                        │  reranked chunks    │  │
│                                        └─────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
    │  WorkflowState (risk_findings, confidence)
    ▼
┌──────────────────────────────────────────────────────────────────┐
│ SynthesizerAgent                                                 │
│ Task: synthesise findings into compliance report                │
│ In:  risk_findings[], query                                     │
│ Out: final_report (markdown — Executive Summary + Findings)     │
└──────────────────────────────────────────────────────────────────┘
    │
    ▼
SSE stream → Browser (per-agent events as each node completes)
    │
    ▼  OTLP HTTP
Arize Phoenix — per-agent spans, latency, full trace
```

**Key:** Each agent runs **once** per query, in sequence. No parallel execution, no voting, no aggregation across models.

### Infrastructure

```
Browser (vanilla JS, no build step)
  ├── GET  /          → single HTML file
  └── POST /workflow/stream → SSE event stream (fetch + ReadableStream)

FastAPI (port 8001)
  └── LangGraph StateGraph
        ├── PlannerNode    → Groq llama-3.1-8b
        ├── AnalystNode    → Quaestor (port 8000) + Groq
        └── SynthesizerNode → Groq
```

The key architectural choice is the `WorkflowState` TypedDict: a single shared data contract threaded through all three LangGraph nodes. No agent stores state. No agent calls another agent. LangGraph merges partial dict returns after each node and routes to the next.

---

## Streaming Pipeline

Each LangGraph node yields its output as an SSE event when it completes. The browser renders each event immediately — no waiting for the full pipeline.

| Event | When emitted | Payload |
|---|---|---|
| `planner_complete` | After PlannerAgent | `task_count`, `confidence` |
| `analyst_finding` | After each finding is parsed | `clause_reference`, `risk_level`, `finding`, `source` |
| `analyst_complete` | After AnalystAgent | `findings_count`, `confidence` |
| `report_complete` | After SynthesizerAgent | `report` (full markdown) |
| `done` | After graph exits | `trace_id` (Phoenix deep-link) |

```
User types query → click Submit
  ├── planner_complete  → "3 tasks planned" card appears
  ├── analyst_finding   → finding card 1 renders (red/amber/green border)
  ├── analyst_finding   → finding card 2 renders
  ├── analyst_finding   → finding card 3 renders
  ├── analyst_complete  → "3 findings, confidence 0.85" badge
  ├── report_complete   → full markdown report appears
  └── done              → "View in Phoenix →" link
```

The UI is a single HTML file with no dependencies. No npm, no CDN, no build step.

---

## Results

### Eval — 30-Case Golden Dataset

The evaluation dataset covers 7 query categories across real regulatory documents (JPMorgan 10-Q, Apple 10-K, IFRS 15, PCAOB AS 2201). Cases include straightforward queries, cross-standard edge cases, ambiguous phrasing, and deliberately hard cases.

| Metric | Value |
|---|---|
| Task completion | **100% (30/30)** |
| Fallback activation | 0% |
| P50 latency | 2055ms |
| P95 latency | 2895ms |
| Gate (≥65%, P95≤3s) | **PASS** |
| Report structure pass | 100% |

![Before vs After — Phase 6 to Phase 7](docs/figures/act4_before_after.png)

*Left: confidence distribution (bimodal → unimodal). Centre: latency box plot (P95 33.6s → 2.9s). Right: key metrics comparison.*

### Phase Progression

Each phase had a numeric exit gate before merging. Test count, eval pass rate, and latency were the criteria.

| Phase | What Was Added | Unit Tests | Eval | Completion |
|---|---|---|---|---|
| 0 | Single-agent rule-based baseline | 18 | 5/5 | 100% |
| 1 | LangGraph 3-agent pipeline (mock) | 62 | 10/10 | 100% |
| 2 | Quaestor integration — real retrieval | 89 | 10/10 | 100% |
| 3 | Tenacity retry logic + fallback events | 120 | 10/10 | 100% |
| 4 | Confidence threshold + task context | 148 | 15/15 | 100% |
| 5 | OTel tracing → Arize Phoenix | 172 | 20/20 | 100% |
| 6 | SSE streaming + LLM key rotation | 202 | 30 cases | 76.7%† |
| **7** | **Web UI + Docker + analyst guardrails** | **207** | **30/30** | **100%** |

![Phase Progression — Completion and Latency](docs/figures/act5_phase_progression.png)

*Left: completion rate per phase. Right: P50/P95 latency on log scale — Phase 2→3 Ollama→Groq 67× drop, Phase 6→7 P95 11.5× improvement.*

†The gate surfaced a real architectural defect that was diagnosed and fixed in Phase 7. Full forensic account in [`docs/case_study.md`](docs/case_study.md).

---

## Stack

| Layer | Technology | Why |
|---|---|---|
| API | FastAPI 0.104+ | ASGI, async-native, OpenAPI docs |
| Orchestration | LangGraph | Explicit graph topology, per-node streaming via `astream()` |
| LLM | Groq `llama-3.1-8b-instant` | 300+ tok/s free tier; one free key |
| Retrieval | Quaestor API | Hierarchical chunking, cross-encoder reranking |
| Validation | Pydantic v2 `extra="forbid"` | ValidationError at boundary > silent data loss |
| Tracing | OpenTelemetry → Arize Phoenix | Per-agent spans, latency breakdown |
| Runtime | Python 3.11, uv | |
| Packaging | Docker Compose profiles | `--profile app` / `--profile monitoring` |

---

## How to Run

### Quick Start (Docker)

Requires both repos as siblings:

**Both repos must be cloned as siblings** — `docker-compose.yml` builds Quaestor from `../quaestor`.

```bash
# 1. Clone both repos as siblings
git clone https://github.com/alex-rosas/quaestor.git
git clone https://github.com/alex-rosas/consilium.git

# Resulting layout:
#   projects/
#     quaestor/   ← document retrieval service (port 8000)
#     consilium/  ← this repo (port 8001)

# 2. Configure
cd consilium
cp .env.example .env
# Edit .env — set GROQ_API_KEY to your key from console.groq.com

# 3. Start the full stack (Quaestor + Consilium + Phoenix)
docker-compose --profile app up
```

| Service | Port | URL |
|---|---|---|
| Consilium | 8001 | http://localhost:8001 |
| Quaestor | 8000 | http://localhost:8000 (built from `../quaestor`) |
| Phoenix | 6006 | http://localhost:6006 |

> **Quaestor** is a separate repository that provides document retrieval (hierarchical chunking, cross-encoder reranking). Consilium calls it at runtime via `QUAESTOR_BASE_URL`. Without it, set `RETRIEVAL_PROVIDER=mock` to run with deterministic test fixtures instead of real document retrieval.

### Local Development

```bash
# Clone both repos first (see Quick Start above)

cd consilium
uv venv && source .venv/bin/activate
uv pip install -e ".[dev]"
cp .env.example .env  # set GROQ_API_KEY

# Terminal 1 — Start Quaestor
cd ../quaestor && uvicorn quaestor.api.main:app --port 8000

# Terminal 2 — Start Consilium
cd ../consilium && uvicorn consilium.api.main:app --port 8001 --reload
```

To skip Quaestor for local dev, set `RETRIEVAL_PROVIDER=mock` in `.env` — all agent logic runs normally with deterministic fixture data.

### Environment Variables

| Variable | Default | Description |
|---|---|---|
| `GROQ_API_KEY` | _(required)_ | Groq API key — get one free at [console.groq.com](https://console.groq.com) |
| `GROQ_MODEL` | `llama-3.1-8b-instant` | Groq model name |
| `RETRIEVAL_PROVIDER` | `mock` | `mock` (no external dependency) or `quaestor` (real retrieval) |
| `QUAESTOR_BASE_URL` | `http://localhost:8000` | Quaestor endpoint |
| `OTEL_EXPORTER_OTLP_ENDPOINT` | `http://localhost:4318` | Phoenix OTLP HTTP |
| `CONFIDENCE_THRESHOLD` | `0.5` | Minimum confidence to accept agent output |
| `TRACE_SAMPLE_RATE` | `1.0` | OTel sampling rate |

---

## Key Engineering Decisions

Full reasoning in [`docs/case_study.md`](docs/case_study.md). Summary:

**1. LangGraph over raw async chaining** — Makes the graph topology explicit, enforces `WorkflowState` as the single data contract, and provides per-node streaming via `astream()` with no additional plumbing. The cost is a dependency on LangGraph's API surface.

**2. Groq free tier** — 300+ tok/s on `llama-3.1-8b-instant`, fast enough that a 30-case sequential eval completes in under 2 minutes with a single free key.

**3. SSE over WebSockets** — One `POST /workflow/stream` request, one persistent response body, no state. WebSockets add bidirectional complexity that isn't needed. The one friction: `EventSource` doesn't support `POST`, so the UI uses `fetch + ReadableStream` instead.

**4. `extra="forbid"` on every Pydantic model** — Established in Phase 0, never relaxed. LLM output that adds unexpected fields raises `ValidationError` at the boundary. In a compliance context, silent data acceptance is a correctness failure.

**5. Phase-gated eval** — Every phase had a numeric exit criterion. The Phase 6 gate failure (76.7%) surfaced a real architectural defect in `AnalystAgent`. Without the gate, the truncation bug would have shipped silently.

---

## Known Limitations

100% on the 30-case golden dataset. Known constraints for queries outside that set:

| Limitation | Impact |
|---|---|
| Retrieval capped at 6 chunks | Broad queries covering many sections get incomplete analysis |
| Cross-standard comparisons | Planner may produce a single task, Analyst retrieves one standard only |
| Numerical precision not guaranteed | LLM may paraphrase exact thresholds from retrieved text |
| No clarification loop | Ambiguous entity references are resolved by picking the most prominent mention |

Root causes, fixes, and the Phase 6 failure forensics: [`docs/case_study.md`](docs/case_study.md).

---

## What Was Intentionally Not Built

| Item | Rationale |
|---|---|
| **Neo4j graph memory** | Cross-document failure gate (≥30% failure) was never triggered — adding graph memory without demonstrated need is complexity for its own sake |
| **LoRA fine-tuning** | Base model accuracy is adequate for structured JSON extraction; fine-tuning requires a labelled dataset and GPU serving pipeline |
| **Auth / API keys** | Local development tool — auth addresses no real threat model here |
| **Paid Groq tier** | Free tier works with `_MAX_ANALYST_CHUNKS=6`; removing the cap is an operational decision |
| **Shared Quaestor/Consilium schema** | `RetrievalResult` is defined independently in both; a shared package requires publishing infrastructure |
| **OTel Collector** | Direct OTLP HTTP to Phoenix is sufficient at single-service scale |

For what *does* break and why, see [Known Limitations](#known-limitations).

---

## Known Technical Debt

1. **SSE error path** — Backend exceptions drop the connection without emitting an `error` event.
2. **Quaestor retrieval not in its own OTel span** — Captured inside the analyst agent span, not as a named child.
3. **`RetrievalResult` schema duplication** — Defined independently in Consilium and Quaestor.
4. **Analyst finding cap** — `_MAX_ANALYST_CHUNKS=6` limits findings to 3 per query. Correct for free-tier Groq; removable with a paid tier.
5. **Analyst LLM call blocks event loop** — `await agent.execute()` is not offloaded to a thread pool. No measurable impact at this scale.

---

## Project Structure

```
consilium/
├── src/consilium/
│   ├── config.py              # All settings via pydantic-settings
│   ├── api/
│   │   └── main.py            # FastAPI: GET /, POST /workflow, POST /workflow/stream, GET /health
│   ├── ui/
│   │   └── index.html         # Single-file vanilla JS UI — no build step
│   ├── workflow/
│   │   └── graph.py           # LangGraph: WorkflowState, PlannerNode, AnalystNode, SynthesizerNode
│   ├── agents/
│   │   ├── planner.py         # PlannerAgent — decomposes query into max 3 tasks
│   │   ├── analyst.py         # AnalystAgent — retrieves chunks, LLM classifies findings
│   │   └── synthesizer.py     # SynthesizerAgent — generates compliance report
│   ├── integrations/
│   │   ├── llm_factory.py     # create_llm_client() — Groq/Ollama backend selection
│   │   ├── quaestor_client.py # QuaestorClient — HTTP retrieval with Tenacity retry
│   │   └── retrieval_mock.py  # MockRetrieval — deterministic test fixture
│   ├── schemas/               # Pydantic models: WorkflowRequest, WorkflowResponse, ComplianceFinding, SSE events
│   └── tracing.py             # init_tracing() — OTel setup, called once in FastAPI lifespan
├── tests/
│   ├── unit/                  # 207 tests — no external services, all offline
│   └── integration/           # Concurrent workflow, real API tests
├── eval/
│   ├── golden_workflows.json  # 30-case frozen dataset (append-only)
│   ├── run_eval.py            # Eval runner: --phase, --store-responses, --golden, --inter-case-delay
│   └── results/               # Timestamped JSON per run
├── demo/
│   ├── app.py                 # Streamlit static demo (3 tabs, no backend required)
│   ├── demo_data.py           # Pre-recorded eval responses + ALL_EVAL_RESULTS
│   └── requirements.txt
├── docker-compose.yml         # --profile app / --profile monitoring
├── Dockerfile
└── .env.example
```

---

## Phases

- [x] Phase 0: Single-agent rule-based baseline (18 tests, 5 cases)
- [x] Phase 1: LangGraph 3-agent pipeline — Planner, Analyst, Synthesizer (62 tests)
- [x] Phase 2: Quaestor integration — real document retrieval (89 tests)
- [x] Phase 3: Tenacity retry logic, fallback event tracking (120 tests)
- [x] Phase 4: Confidence threshold gate, task context propagation (148 tests)
- [x] Phase 5: OpenTelemetry tracing → Arize Phoenix (172 tests)
- [x] Phase 6: SSE streaming, Groq LLM key rotation (202 tests, 30-case eval)
- [x] Phase 7: Web UI, Docker Compose, analyst prompt guardrails — **30/30 eval, 100%** (207 tests)

---

## Case Study

[`docs/case_study.md`](docs/case_study.md) — Engineering decisions, failure modes, and project positioning.

Covers: why LangGraph over raw chaining, the `EventSource` POST problem, how the 76.7% eval failure was diagnosed (three wrong hypotheses before the right one), positioning vs LangChain simple pipelines and CrewAI, and what would be built differently starting fresh.
